/**
 * First we will load all of this project's JavaScript dependencies which
 * includes React and other helpers. It's a great starting point while
 * building robust, powerful web applications using React + Laravel.
 */

require('./bootstrap');

/**
 * Next, we will create a fresh React component instance and attach it to
 * the page. Then, you may begin adding components to this application
 * or customize the JavaScript scaffolding to fit your unique needs.
 */


/**
 *
 * About page
 *
 *  Tabs
 *      > General
 *      > Header
 *      > Sections
 *
 * General contains general settings:
 *  - checkbox for "Show 'Post a Comment' form
 *
 * Header contains a form:
 *  - title
 *  - subtitle
 *  - description
 *  - image
 *
 * Sections will contain a button for adding a new section and a form including the following:
 *
 *  - section title
 *  - section subtitle
 *  - section description
 *  *  - a checkbox "Has subsections"
 *  - a subsections box with an add button for adding a subsection which can contain:
 *      - title
 *      - content
 *  - a checkbox "Has image" - if does, then an file input will appear.
 *  - a checkbox "Has image slider", if does, then an file input will appear.
 *  - a checkbox "Has text-boxes" - if does then this form will appear:
 *      - header-text
 *      - title
 *      - content
 *
 *
 */
